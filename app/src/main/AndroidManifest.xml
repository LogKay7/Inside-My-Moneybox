<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onetirelire">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_onetirelire"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Transac"></activity>
        <activity android:name=".ConfirmRemoveMoney" />
        <activity android:name=".RemoveMoney" />
        <activity android:name=".Details" />
        <activity android:name=".RecapAddPieces" />
        <activity android:name=".AddPieces" />
        <activity android:name=".ConfirmAdd" />
        <activity android:name=".ActivityRecapAddBillets" />
        <activity android:name=".ActivityAddBillets" />
        <activity android:name=".ChoiceMoneyAddActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>